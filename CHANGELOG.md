# سجل التغييرات — نبض (نظام إدارة مركز التدريب)

## [1.0.0] — 2026-08

أول إصدار «منتج جاهز للسوق» بعد مشروع التحول الشامل (المراحل 0→7). كل مرحلة في
التزام منفصل قابل للرجوع، والمنطق المالي/المحاسبي القائم لم يُلمس في أي مرحلة تصميم.

### أمان الخادم
- سدّ تجاوز المصادقة الثنائية: مسارا رابط الإيميل والبصمة يفرضان كود TOTP وفحص قفل
  الحساب الآن كمايفعل الدخول العادي (`9ce58e2`).
- حدود معدل على 4 مسارات كانت مفتوحة (تحقق الرابط، خيارات/تحقق البصمة، إنشاء/متابعة QR)
  + تنظيف دوري يومي لجدول روابط الدخول (`4841ae4`).
- جلسة انتظار قصيرة العمر تُستهلك مرة واحدة لدخول البصمة مع TOTP؛ حوار إدخال الكود
  بالواجهة للتدفقين دون تغيير مستدعياتهما.
- تدقيق أمني موثق بدليل السطر: `docs/redesign/07-security-audit.md`.

### الهوية والتصميم
- نظام Design Tokens كانوني (فاتح/داكن) مع طبقة aliases تضمن تطابق 45 توكناً قديماً —
  تحقق آلي بمطابقة سلاسل `var()` على الجانبين.
- App Shell: مجموعات تنقل معنونة (التشغيل/المالية/الذكاء)، وضع Rail افتراضي بتلميحات،
  شريحة الفترة المالية في الهيدر، مؤشر حالة البث اللحظي.
- ترقية البحث الشامل إلى **مركز أوامر** (تنقل + إجراءات سريعة + تنقل بالأسهم/Enter).
- تدقيق تباين WCAG آلي؛ تصحيح شبه غير محسوس للكهرماني والفيروزي إلى 3.1:1+ للأبيض العريض.
- احترام `prefers-reduced-motion` عالمياً.

### لوحة القيادة
- **نبض القيادة**: تدفق نقدي يومي آخر 30 يوماً (SVG خفيف)، سجل نشاط مباشر بزمن نسبي،
  وإجراءات سريعة حسب الصلاحيات — فوق شبكة CFO القائمة ومن محور تحديثها نفسه.

### مساحات العمل (Workspaces)
- **ملف العميل**: درج جانبي بنقر مزدوج أو من قائمة الصف — وضع مالي بشريط سداد، بطاقات
  هوية/دورة/حقائب، آخر الحركات المرتبطة، وأزرار تعيد فتح النماذج القائمة نفسها.
- **تفاصيل الحركة المالية**: كل الحقول + شارات الحالة بكاشفات الشاشة الرسمية + سياق رصيد
  الحساب وأثر الحركة.
- **محرر القيود**: مؤشر توازن حي (مقياسان متناسبان + شارة حالة) وزر «سدّ الفرق تلقائياً».
- **معاينة المستند للمشتريات**: ورقة مستند حية بنفس أرقام VAT حرفياً، تتحدث أثناء الكتابة.
- **استوديو التقارير**: 12 بطاقة تشغّل التقارير القائمة عبر أزرارها الحقيقية نفسها.

### الشبكات والحالات
- إظهار/إخفاء 13 عموداً في جدول العملاء مع حماية الأعمدة الثابتة وحفظ التفضيل محلياً.
- عروض فلاتر محفوظة (Saved Views) تعيد تنفيذ خط الفلترة القائم بأحداثه الأصلية.
- قائمة سياق بالنقر الأيمن لصفوف العملاء والخزنة تعيد استخدام قائمة الصف العالمية.
- حالة فراغ موصوفة بزر مسح فوري لكل الفلاتر.

### التهيئة والتوثيق
- معالج ترحيب للاستخدام الأول (3 خطوات) يكتب عبر مسار حفظ الإعدادات الرسمي ولا يظهر
  إلا للتركيبات الفارغة.
- حزمة وثائق التصميم والتسويق: `docs/redesign/00→09` تشمل سكربت عرض تجاري 5 دقائق،
  سياسة مفتاح التشفير وصياغته التعاقدية، وخطة Zero-Knowledge الاختيارية، وقائمة اختبار
  ما بعد النشر.
- إزالة 164 صفاً من بيانات عملاء حقيقية من شجرة العمل (كانت ضمن ملفات seed/demo).

### ملاحظات نشر
- انشر الخادم والواجهة **معاً** (دفعة B تغيّر عقود استجابة مساري magic-link/webauthn).
- نفّذ قائمة الاختبار اليدوية بعد أول نشر: `docs/redesign/09-release-qa-checklist.md`.
